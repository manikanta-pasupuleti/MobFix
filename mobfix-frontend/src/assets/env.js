(function (window) {
  // Runtime configuration for the frontend.
  window.__env = window.__env || {};
  // Set API_URL to the backend URL
  window.__env.API_URL = 'https://mobfix-backend.onrender.com/api';
})(this);
