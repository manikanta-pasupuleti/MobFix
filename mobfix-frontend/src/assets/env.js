(function (window) {
  // Runtime configuration for the frontend.
  // Edit this file on the server to change runtime values without rebuilding.
  // Example (set on the host):
  // window.__env = window.__env || {}; window.__env.API_URL = 'https://api.example.com/api';
  window.__env = window.__env || {};
  // Leave API_URL undefined to use the app's built-in defaults (localhost or relative /api).
  // window.__env.API_URL = 'http://localhost:5000/api';
})(this);
